# Dev image: install deps; source is mounted at runtime for hot reload.
FROM node:20-bookworm-slim

WORKDIR /app

ENV NODE_ENV=development
ENV HOSTNAME=0.0.0.0
ENV WATCHPACK_POLLING=true

COPY package.json package-lock.json ./
RUN npm install

CMD ["npm", "run", "dev"]
